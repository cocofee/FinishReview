# FinishReview 回放性能长期优化方案

> 日期：2026-08-28
> 仓库：`C:\Users\Administrator\Documents\trae_projects\video analysis\FinishReview`
> 分支：`main`
> 状态：播放阶段 0-3、索引缓存、共享冲刺片段、事件绑定和同片段播放器复用已实施；现场笔记本验收待完成

## 当前实施进度

- 已新增 `tools/benchmark_video_playback.py`，可直接测量顺序解码、随机定位、反向窗口边界延迟和后台预取后的实际显示帧率。
- 已完成拖动请求 80 ms 节流、latest-wins 合并和松手后的最终精确定位。
- 已禁止拖动期间的全分辨率重复解码。
- 已增加 worker request generation，防止晚到的旧帧覆盖最新画面。
- 已将反向窗口缩短为约 0.5 秒，并在缓存剩余约一半时使用独立 capture 后台预取下一窗口。
- 单个普通视频 worker 的缓存上限已收紧为 64 MiB；两个窗口共享该预算，非活动窗格暂停时立即释放缓存。
- 已将窗格内播放、逐帧、拖动和空格键改为默认只操作当前画面；顶部“联动”按钮保留双路同步能力。
- 已完成双画面主时钟连续同步：启动时精确对齐一次，随后两个 worker 独立连续解码。
- 30 ms 定时器只推进共享时间和检查漂移，不再每次双路 pause + seek。
- 漂移校正具有启动保护和冷却时间，只校正超过容差的单个窗格；暂停后两路统一精确定位最终帧。
- 已为 `VideoTimelineStore` 增加按赛事的增量时间区间索引、resolved path 索引、短期可播放性缓存和 O(1) timeline revision。
- 已新增 `realtime/review_clip.py`，持久化共享 `ReviewClip` 和按 `(event_id, revision, camera_index)` 查询的 `PassageReviewBinding`。
- 同一刷新周期内重叠且总长度不超过 20 秒的 READY 窗口合并为一个共享清单，binding 使用一次批量 journal 写入。
- 正常新事件优先直接读取 binding；旧测试数据和缺少 binding 的机位继续使用时间线 fallback。
- 同一 clip 内切换运动员复用现有 worker，只改变片内偏移；archive 新增时只刷新时间范围内且缺少 binding 的事件。

2026-08-28 使用完整 663 人测试目录进行只读对比：

- 旧实现完成 663 条录像定位需要 18.06 秒，平均 27.23 ms/人。
- 新时间索引首次完成相同定位需要 1.06 秒，平均 1.61 ms/人。
- 文件状态缓存命中后需要 0.74 秒，平均 1.11 ms/人。
- 新旧结果均为 662 条 `located`、1 条 `recording_gap`、1324 个机位位置，定位语义未改变。
- 固定测试已覆盖 5 秒内 50 人：单机位只生成 1 个共享 clip，50 条 binding 合并为一次 journal 写入。

现场笔记本可执行以下命令，将实际录像替换为对应文件路径：

```powershell
python -m tools.benchmark_video_playback "D:\race\videos\camera_01.mkv" --output playback-benchmark.json
```

## 一、问题定义

现场笔记本为 8 GB 内存、128 GB SSD 加机械硬盘。FinishReview 在 4070 开发机上基本正常，但在现场笔记本上存在以下问题：

1. 鼠标拖动画面进行终点判别时明显卡顿，预览不能连续跟随。
2. 向后播放时基本不连续，表现为停顿、跳帧和低帧率。
3. 双画面联动正向回看也会一卡一卡。
4. 内存占用约 78%，但 FinishReview 单进程没有显示出明显泄漏。

本方案的目标是在不增加 NVR、不依赖升级内存或购买高性能电脑的前提下，通过播放架构改造，使普通现场笔记本能够稳定完成一到两路终点视频的拖动、逐帧、正向和反向判读。

本方案不降低正式证据文件质量。低分辨率只用于拖动预览，暂停后的最终判读帧仍来自原始视频。

## 二、当前根因

### 1. 反向播放逐帧随机定位

`realtime/video_playback.py:449-474` 的 `_decode_target()` 只优化小范围向前读取。向后目标不在缓存中时，每帧都会执行：

```python
capture.set(cv2.CAP_PROP_POS_FRAMES, target)
capture.read()
```

H.264/H.265 的普通帧依赖前面的关键帧。随机定位一帧时，解码器通常需要回到关键帧并重新解码。H.265、长 GOP 和机械硬盘会进一步放大成本。

### 2. 双画面同步使用高频 pause + seek

`realtime/passage_review.py:1556-1558` 将同步定时器设置为 30 ms。`_on_sync_tick()` 每次调用 `_seek_both_delta()`，随后两个窗格都执行 `worker.pause()` 和 `worker.seek()`。

这不是真正的连续播放，而是每秒约 33 次双路随机定位。强电脑能够较快完成这些操作，弱电脑会直接表现为间歇播放。

### 3. 拖动请求没有节流和合并

`PassageEvidenceView.mouseMoveEvent()` 会在每次鼠标移动时发出 `scrub_delta_requested`。请求随后立即转换为两个窗格的 seek。

虽然 worker 的 pending seek 能覆盖部分旧请求，但已经开始的解码无法取消，最终形成大量无效随机读取。

### 4. 暂停状态重复读取全分辨率帧

`PassageEvidencePane._on_frame_ready()` 在暂停且缩放不低于 100% 时，会立即请求同一帧的全分辨率版本。拖动期间可能出现一次预览解码加一次全分辨率解码。

### 5. 没有明确启用硬件解码

`_open_video_capture()` 只是普通 `cv2.VideoCapture(path)`，没有配置硬件加速。因此开发机正常不能证明程序使用了 RTX 4070，更可能是开发机的 CPU、NVMe 和整体性能掩盖了随机定位成本。

## 三、设计原则

1. **证据优先**：原始录像、事件时间和最终选帧语义不变。
2. **连续解码优先**：能顺序读取时不随机 seek。
3. **最新请求优先**：拖动时允许丢弃过期预览请求，不能延迟最终目标帧。
4. **预览与判读分离**：交互过程中追求响应，停止交互后追求原始分辨率。
5. **弱电脑为基线**：以 8 GB 内存、SSD、无独立显卡为主要验收环境。
6. **资源有上限**：缓存必须有全局预算，不能按窗格无限叠加。
7. **可分阶段回退**：每阶段都能独立验证，不一次性替换全部播放路径。

## 四、目标架构

### 1. 播放请求分级

在播放窗格和 worker 之间区分三类请求：

| 请求 | 用途 | 行为 |
|---|---|---|
| `PREVIEW_SEEK` | 鼠标拖动 | 合并旧请求、低分辨率、允许跳过中间位置 |
| `EXACT_SEEK` | 松开鼠标、逐帧、选帧 | 精确到目标帧、原始分辨率、不得丢弃 |
| `CONTINUOUS_PLAY` | 正向或反向播放 | 以连续解码和缓存消费为主，不逐帧 seek |

worker 为请求分配递增 generation。完成解码时，如果结果已落后于最新 generation，则不再提交到 UI。

### 2. 拖动预览管线

拖动开始后：

1. 暂停连续播放。
2. 暂停全分辨率请求。
3. 保存最新目标时间，使用 66-100 ms 定时器合并请求。
4. 最多每秒显示 10-15 个预览位置。
5. 使用最大 1280 像素宽的预览图像。

拖动结束后：

1. 清除未开始的预览请求。
2. 只对最终位置执行一次 `EXACT_SEEK`。
3. 精确帧到达后再允许全分辨率显示和标记操作。

### 3. 反向窗口缓存

反向播放不再对每个目标帧执行随机定位。

当目标帧未命中缓存时：

1. 计算目标帧前 1-2 秒的窗口起点。
2. 一次 seek 到窗口起点附近。
3. 顺序解码到目标帧。
4. 将窗口内预览帧写入有界缓存。
5. 从缓存末端向前连续显示。
6. 接近缓存头部时，后台准备前一个窗口。

第一阶段可以继续使用 OpenCV 完成窗口解码。若真实 H.265 样本仍不能满足指标，再引入项目已经携带的 FFmpeg/ffprobe 后端，建立关键帧索引并从关键帧开始顺序解码，不新增大型第三方依赖。

### 4. 双画面主时钟同步

`PassageReviewDialog` 保留共享的 passage delta，但不再每 30 ms seek 两个 worker。

新的同步方式：

1. 启动时设置共享 `PlaybackClock`，记录起始 delta、方向和速度。
2. 两个 worker 分别连续解码。
3. UI 根据时间戳选择当前应显示的最近帧。
4. 偏差在一帧以内时不校正。
5. 落后时允许丢弃预览帧追赶，不丢弃暂停后的最终精确帧。
6. 偏差持续超过一帧或发生用户跳转时，才执行一次校正 seek。

不同帧率的视频按时间戳同步，而不是假设两个视频具有相同帧号。

### 5. 全局缓存预算

当前实现不再允许每个 worker 独占 160 MB，而是采用可直接验证的固定上限和活动窗格释放策略。

建议初始规则：

- 单个普通视频 worker 上限：64 MiB。
- 默认只播放当前活动窗格，其他窗格暂停并释放播放缓存。
- 显式开启两个普通视频联动时，合计缓存上限约为 128 MiB。
- 窗格切换、方向变化、拖动或视频切换时立即取消无用预取。
- 全分辨率只保留当前精确帧和少量相邻帧。
- 缓存按实际 `QImage.byteCount()` 计费。

该预算后续应通过现场数据调整，而不是继续按窗格固定分配。

### 6. 活动窗格优先

判读操作在任意时刻通常只聚焦一个窗格。因此：

- 活动窗格显示原始分辨率精确帧。
- 其他同步窗格播放时使用预览分辨率。
- 全部窗格暂停后，可按需加载各自精确帧。
- 原始录像始终完整保留，不改变证据文件。

## 五、实施阶段

### 阶段 0：建立真实性能基线

新增独立性能工具，不修改正式判读语义：

- 使用 FFmpeg 生成带帧号和时间码的固定样本。
- 覆盖 H.264/H.265、1080p/1440p、25 fps、GOP 25/50。
- 使用真实 `cv2.VideoCapture`，不能只用 fake capture。
- 分别在 SSD 和机械盘记录结果。

采集以下指标：

- 单次随机 seek 的 median/p95 延迟。
- 拖动期间已发请求、实际解码和最终显示数量。
- 正向与反向的实际显示帧率。
- 双窗时间偏差。
- UI 主线程最长阻塞。
- 进程工作集、私有内存和缓存字节数。

### 阶段 1：拖动请求治理

范围：`realtime/passage_review.py`、`realtime/video_playback.py` 及对应测试。

改动：

1. 增加 scrub finished 信号。
2. 对拖动请求进行节流和 latest-wins 合并。
3. 拖动期间不触发 full-resolution 请求。
4. 松开鼠标后只提交一次精确请求。
5. 使用 generation 丢弃过期解码结果。

该阶段风险较低，应最先实施，并可直接改善拖动卡顿。

### 阶段 2：反向块解码

范围：主要修改 `realtime/video_playback.py`。

改动：

1. 将当前单帧 LRU 扩展为方向感知的窗口缓存。
2. 反向缓存未命中时批量顺序解码。
3. 在缓存接近耗尽时预取前一个窗口。
4. 速度改变、跳转和视频切换时正确取消预取。
5. 保留逐帧精确定位语义。

该阶段是解决反向播放不连续的核心。

### 阶段 3：双画面连续同步

范围：`realtime/passage_review.py` 和 worker 播放控制接口。

改动：

1. 引入共享播放时钟。
2. 两个 worker 连续播放，不再由 30 ms 定时器反复 seek。
3. 通过时间戳和容差纠偏。
4. 支持不同帧率、不同可用时间范围和单窗缺失。
5. 保持暂停、逐帧、标记和 passage delta 的现有语义。

### 阶段 4：FFmpeg 关键帧索引和可选硬解

只有阶段 1-3 在真实笔记本上仍未达到指标时才进入本阶段。

改动方向：

1. 使用 ffprobe 建立关键帧时间和文件位置索引。
2. 使用 FFmpeg 从最近关键帧顺序输出预览帧。
3. 自动探测可用硬解能力，失败时回退软件解码。
4. 不把 NVIDIA GPU 作为运行前提，兼容 Intel 核显和无硬解环境。

不建议第一步就全面替换 OpenCV 播放后端，否则会同时扩大打包、时间戳、进程管理和回归测试范围。

### 阶段 5：HLS 和磁盘后台整理

在核心播放链路完成后再处理次要压力：

- 播放列表未变化时跳过扫描。
- 只处理新增媒体序号。
- 文件存在性检查、清理和归档发布移出 UI 主线程。
- 避免录制和判读同时反复扫描全部片段。

## 六、验收标准

主要验收设备：8 GB 内存现场笔记本，录像和 review buffer 位于 SSD。

主要测试配置：一路和两路 H.264、1080p、25 fps、GOP 25/50。H.265 和 1440p 作为压力测试。

### 交互指标

- 连续拖动时，预览更新 p95 不超过 150 ms。
- 松开鼠标后，SSD 上精确帧 p95 不超过 400 ms。
- 拖动停止后不能继续播放旧请求产生的过期画面。
- 标记保存的帧号和时间戳必须与最终显示帧一致。

### 播放指标

- 正向 1x 不出现周期性 pause + seek 停顿。
- 反向 0.25x、0.5x、1x 按时间顺序连续显示。
- 反向 0.5x 在 25 fps 视频上至少显示 90% 的应显示帧。
- 双窗同步偏差 p95 不超过各自较长的一帧时间。
- 到达片段边界、某一路缺失或长度不同时不能卡死。

### 资源指标

- 播放缓存总量始终不超过配置预算。
- 连续拖动 10 分钟后内存回到稳定区间，不持续增长。
- UI 主线程不执行视频解码、文件全量扫描或阻塞式等待。
- GPU 硬解不可用时仍能使用软件解码完成判读。

## 七、测试要求

### 单元测试

- 高频 scrub 只处理限速后的最新目标。
- scrub release 一定产生最终精确请求。
- 过期 generation 结果不能覆盖新画面。
- 反向缓存命中不调用随机 seek。
- 缓存未命中只进行一次窗口 seek 和顺序读取。
- 全局缓存超过预算时按策略淘汰。
- 双窗偏差小于容差时不执行校正 seek。
- 视频切换和窗口关闭时 worker、预取任务和 FFmpeg 子进程能退出。

### 集成测试

- 使用带帧号的真实 H.264/H.265 文件验证正向、反向和精确定位。
- 验证 25 fps 与不同帧率视频的时间戳同步。
- 验证双窗中一路到达边界后的行为。
- 验证暂停后全分辨率帧没有替换成错误的旧请求。

### 现场验收

- 必须看笔记本本地屏幕，不能只根据网易 UU 远程画面判断流畅度。
- 同时记录 CPU 各逻辑处理器、磁盘活动时间、GPU Video Decode 和内存。
- 分别测试停止录制后的回看以及边录边判，区分解码和磁盘竞争。

## 八、摄像机和存储建议

软件改造不要求购买新电脑，但编码设置仍会影响弱设备：

- 优先 H.264，25 fps。
- GOP 建议 25 帧，最多 50 帧。
- 优先 1920x1080；细节不足时再测试 2560x1440。
- 正式证据与 review buffer 放 SSD，机械盘用于赛后归档。
- 不能以低清子码流替代正式证据；子码流只能作为可选预览来源。

## 九、风险与回退

1. **精确帧风险**：OpenCV 对部分编码的帧号定位可能不精确。测试视频必须烧录帧号，最终以实际画面和时间戳验证。
2. **同步语义风险**：普通视频和高速视频帧率不同。同步必须基于 passage 时间，不基于帧号。
3. **缓存风险**：缓存只保存显示帧，不改变或覆盖原始文件。
4. **并发风险**：旧解码结果可能晚于新请求返回，必须使用 generation 防止旧画面回写。
5. **打包风险**：若启用新的 FFmpeg 运行方式，必须重新验证 PyInstaller 路径解析和进程退出。
6. **回退方式**：阶段 1-3 保留原 worker 工厂注入边界；新引擎在完成现场验收前保留 legacy 回退入口。

## 十、663 人压力测试的正式结论

### 1. 数据性质

本次样本不是正式比赛，也不是 CycleRace 在运动员正式完赛后重复推送过线。它由多个分开的测试时段组成，CycleRace 复用了相同的 `event_id` 并提高 `revision`：

- Passage journal 共 1327 行，当前逻辑事件 663 条。
- revision 1 共 663 条，接收时间为 2026-08-27 16:33:05 至 17:08:43。
- 第一轮结束后空闲约 17 分 21 秒。
- revision 2 共 663 条，接收时间为 2026-08-27 17:26:04 至 17:28:42。
- revision 3 只有 1 条，接收时间为 2026-08-28 18:03:10，距离第二轮结束约 24 小时 34 分钟。
- CycleRace 已在源头决定是否推送正式过线。运动员完赛后再次刷芯片但不产生正式过线时，FinishReview 不会收到事件，也不需要增加“重复过线”协议。

因此，FinishReview 保留 663 条最新事件是正确行为。问题发生在录像派生层：同一逻辑事件每次修订都创建新的、按运动员独占的录像清单，旧清单继续留在时间线中。

### 2. 真实规模

本次核对使用的完整赛事目录为：

`C:\Users\Administrator\Desktop\2026“宁夏银行杯”中国文旅名城骑行乐游大会（宁夏站）_暨首届“丝・麓・山海情”贺兰山东麓葡萄酒自行车公开赛`

目录和时间线具有以下规模：

| 项目 | 数量 |
|---|---:|
| 总容量 | 9.765 GB |
| `videos` 容量 | 6.539 GB |
| `review_buffer` 容量 | 3.222 GB |
| MKV 文件 | 64 |
| TS 文件 | 4516 |
| M3U8 文件 | 2823 |
| `evidence_*.m3u8` | 2652 |
| `preview_*.m3u8` | 163 |
| 连续录像 M3U8 | 8 |
| 时间线 journal 行数 | 5388 |
| 完整录像段 | 2694 |
| `passage_review_window` | 2652 |
| `continuous_archive_fallback` | 42 |
| 机位 1 录像段 | 1347 |
| 机位 2 录像段 | 1347 |
| 当前事件 | 663 |

文件完整性核对结果：

- 2694 个完整时间线段引用 2690 个不同路径，缺失路径为 0。
- 2652 个 evidence 清单全部可读取，缺失 TS 引用为 0。
- 2648 个 evidence 清单各引用 4 个 TS，另有 4 个各引用 5 个 TS。
- 2652 个 evidence 清单只有 73 种不同的 TS 序列签名，2579 个清单属于重复签名。
- 同一 TS 序列最多被 50 个不同 evidence 清单重复引用；全部 evidence 清单实际只涉及 219 个不同 TS 文件。

过线时间具有真实冲刺压力：

- 150 ms 内最多 50 人具有高度重叠的正式过线时间。
- 1 秒内最多 50 人。
- 5 秒内最多 100 人。
- FinishReview 实际 150 ms 接收批次最大只有 5 条，因此 `>64` 条批次回退全刷新不是本样本的主要原因。

### 3. 已量化的热点

当前首次为 663 条事件定位录像时：

- 扫描约 1,786,122 次时间线段。
- 产生约 73,641 次候选录像可播放性检查。
- 每条事件平均命中约 111 个重叠候选，p95 为 147，最大 147。
- 73,641 次检查实际只涉及约 1330 个不同路径。
- 2652 个独立复核清单实际只有 73 组不同的 TS 序列，全部清单只引用 219 个不同 TS 文件。

发布阶段还存在以下放大效应：

- 每个运动员、每个机位写入一个独立 `.m3u8`。
- 每次写播放清单执行一次文件 `fsync`，每次追加时间线再执行一次 `fsync`。
- 2652 个复核清单对应约 5304 次派生文件同步写盘。
- `PassageReviewTimelinePublisher.publish()` 发布新路径前线性扫描已有时间线，理论累计约 351 万次路径比较。
- 每个运动员的清单路径不同，即使底层 TS 完全相同，切换运动员也无法稳定复用已有播放器和帧缓存。

这解释了为什么新建测试赛事后明显变快，也解释了为什么 16 GB SSD 笔记本仍会卡。问题主要是同步磁盘 I/O、重复媒体清单和重复索引查找，不是物理内存不足。实际赛事目录中的时间线、evidence 清单和 TS 引用均完整，而且 2652 个独立清单高度复用同一批 TS，这进一步证实共享 `ReviewClip`、直接事件绑定和播放器复用是正确方向。优化后的真实解码延迟和交互响应仍需在 16 GB SSD 与 8 GB 现场笔记本上分别验收。

## 十一、正式目标架构：共享冲刺片段

### 1. 核心模型

停止为每名运动员生成永久独占的复核播放清单。录像层改为三层：

1. `ReviewSegment`：摄像头连续产生的约 2 秒 TS 物理片段，保持现有证据质量。
2. `ReviewClip`：将时间上相邻、复核区间重叠的运动员合并为一个不可变共享播放清单。
3. `PassageReviewBinding`：把事件及其 revision、机位和过线时间偏移绑定到 `ReviewClip`。

推荐新增独立模块 `realtime/review_clip.py`，避免继续扩大 `review_window.py` 和 `passage_review.py`。建议的数据结构为：

```text
ReviewClip
  clip_id
  race_id
  camera_index
  source_id
  started_at_ms
  ended_at_ms
  playlist_path
  segment_signature
  state

PassageReviewBinding
  event_id
  revision
  camera_index
  clip_id
  passage_timestamp_ms
  passage_offset_ms
  is_active
```

绑定存储必须追加写入并保留 revision 历史。读取时为 `(event_id, revision, camera_index)` 建立字典，并另建每个事件的当前活动绑定。旧 revision 不删除，只退出普通定位路径。

### 2. 冲刺区间合并算法

每个过线事件形成 `[passage - pre_roll, passage + post_roll]` 区间。按比赛和机位维护待封口的区间集合：

1. 新区间与当前 `ReviewClip` 重叠时合并。
2. 合并后总长度不超过 20 秒时继续共享。
3. 超过 20 秒时在 HLS 片段边界分割，但任何运动员的完整前后窗口都不能被截断。
4. 只有 ring buffer 已录到区间结束，并且没有待处理事件需要继续扩展时才封口。
5. 晚到事件如果已被现有片段完整覆盖，直接增加绑定；否则创建新的重叠片段，不修改已封口证据。
6. 共享判定必须基于有序 TS 片段签名、机位、录像源和比赛，不能只按显示时间或文件名去重。

按本次样本计算：

- revision 1 在每个机位约形成 13 个 20 秒以内的片段。
- revision 2 在每个机位约形成 7 个片段。
- revision 3 在每个机位形成 1 个片段。
- 两个机位、三轮测试合计约 42 个共享片段，而不是 2652 个独占清单。
- 只看当前 663 条最新事件，两个机位约有 16 个活动片段。
- 50 人在 5 秒内冲线时，通常只需要每个机位一个约 11 秒的共享片段。

### 3. 直接事件绑定

正常 CycleRace 事件不再通过 `VideoTimelineStore.locate_passage()` 扫描时间线。查询顺序调整为：

1. 使用 `PassageReviewBindingStore` 按事件 revision 和机位 O(1) 查找共享片段。
2. 找到活动绑定后直接创建 `PassageVideoLocation`，位置等于 `passage_offset_ms`。
3. 只有旧测试数据、外部导入录像和没有绑定的兼容数据才进入时间线查找 fallback。

时间线 fallback 仍需要按 `(race_id, source_id/camera_index)` 建立有序区间索引。建议使用排序起点数组、`bisect` 和 prefix-max-end 辅助数组实现 `O(log S + K)` 查询，不新增 interval-tree 依赖。

### 4. 路径缓存和发布索引

即使正常事件改用直接绑定，仍需完成以下基础优化：

- `VideoTimelineStore` 建立 resolved path 到 segment 的字典，发布幂等检查从 O(S) 改为 O(1)。
- 一次刷新内共享 `_video_path_is_playable()` 结果，避免同一路径重复读取清单和检查 TS。
- 已封口片段按路径、大小和修改时间缓存；活动清单在写入或媒体序号变化时失效。
- archive 新增长录像段时，只更新其时间范围内且没有直接活动绑定的兼容事件，不再刷新整场比赛。
- 文件检查、播放清单发布和派生时间线写入离开 UI 主线程，由单写入队列顺序执行。
- 正式 Passage journal 继续保持 durable-before-callback，不为减少 `fsync` 而降低计时数据持久性。

### 5. 播放器复用

播放器的复用键从“运动员独占播放清单路径”变为 `(camera_index, clip_id)`：

- 同一冲刺片段内切换运动员只改变 `passage_offset_ms`，不重新创建 worker。
- 当前 64 MiB 帧缓存继续保留并服务同一片段内的后续运动员。
- 切换到新的共享片段时才释放或停放旧 worker。
- 活动机位优先预取；非活动机位暂停并限制缓存。
- 正向播放仍使用顺序解码，逐帧优先命中现有前后窗口缓存。

该改动直接针对“切换运动员响应慢”和“目标点附近逐帧重新卡住”，收益高于单纯增加内存或更换 SSD。

## 十二、双机位并排判读方案

放大后使用机位 1、机位 2 并排的非模态窗口，但不恢复双路同步逐帧：

- 双击任一普通机位或按 `F` 打开并排判读窗口。
- 两个机位保持同一运动员和共享 passage delta。
- `1`、`2` 或点击画面选择活动机位。
- `Left/Right` 只让活动机位前后 1 帧。
- `Shift+Left/Right` 只让活动机位前后 5 帧。
- 连续按键期间非活动机位保持静止。
- 停止操作约 150 ms 后，非活动机位只同步一次最终时间位置。
- 单机位大画面按 `Esc` 返回并排；并排窗口再按 `Esc` 返回主界面，不关闭整个程序。
- 两个机位分别保留缩放和适应窗口；不恢复鼠标时间轴拖动。

实现时必须移动和复用现有 `PassageEvidencePane`，不能在弹窗中创建第二套播放器。并排显示本身不是主要负载，同时逐帧和同时随机 seek 才是应避免的负载。

## 十三、实施阶段

### 阶段 A：特征测试和压力基线

范围：测试和 benchmark，不改变生产语义。

- 从本次真实分布生成脱敏固定样本：663 当前事件、1327 revision journal、双机位、三轮测试和 50 人冲刺。
- 记录旧实现的启动、全刷新、同片段切换、跨片段切换、逐帧、文件检查、主线程最长阻塞和内存。
- 增加发布计数器：生成清单数、timeline 写入数、`fsync` 次数和 worker 重建次数。
- 固定当前 `locate_passage()` 结果作为差分基线。

### 阶段 B：索引和可播放性缓存

实施状态：**已完成**。

涉及文件：`realtime/video_timeline.py`、`realtime/review_recorder.py`、相关测试。

- resolved path 索引。
- 按比赛、机位和时间的 fallback 索引。
- 一次刷新路径可播放性缓存和明确失效规则。
- archive 时间范围更新，不再全场刷新。

该阶段保持现有文件格式，风险最低，也为新模型提供 fallback。

### 阶段 C：共享 `ReviewClip` 和事件绑定

实施状态：**已完成核心路径**。新录像直接使用共享 clip/binding；旧测试赛事保留 timeline fallback，不执行破坏性迁移。

涉及文件：新增 `realtime/review_clip.py`，调整 `review_recorder.py`、`review_window.py`、`passage_review.py`。

- 新增 clip/binding append-only store。
- 实现 20 秒上限的区间合并和封口状态机。
- 每个共享片段只写一次清单和一次派生索引。
- revision 更新活动绑定，旧绑定保留审计。
- 新事件优先直接绑定，兼容数据继续 fallback。

这是解决 663 人压力样本的核心阶段。

### 阶段 D：播放器复用和双机位并排

实施状态：**同片段 worker 复用已完成**。现有双机位并排、活动机位、缩放、全屏和 `Esc` 交互保持不变。

涉及文件：`realtime/passage_review.py`、`realtime/video_playback.py`、对应 Qt 测试。

- worker 按 clip 复用。
- 同片段切换只移动时间偏移。
- 实现并排窗口、活动机位选择和延迟单次同步。
- 保持现有逐帧、标记、缩放、全屏和 `Esc` 语义。

### 阶段 E：现场验收和条件后端评估

实施状态：**待现场执行**。

- 先在 16 GB SSD 笔记本完成完整压力测试。
- 再在 8 GB 现场笔记本验证 SSD 和机械盘差异。
- 只有共享片段和播放器复用完成后，真实 H.264/H.265 样本仍不达标时，才对 mpv 或 FFmpeg 关键帧后端做独立 A/B 原型。
- 不直接引入 MediaMTX 后台服务，不在未测量前替换现有 OpenCV 播放后端。

## 十四、正式验收标准

### 1. 数据和证据正确性

- 663 条当前事件始终保持 663 条，不因 revision 产生重复可见行。
- 相同 revision 重发不生成新绑定或新清单。
- 新 revision 只替换活动绑定，旧绑定和旧录像仍可审计。
- 每个活动事件对每个已配置机位最多一个活动绑定。
- 共享片段不能共享运动员确认状态、选帧或证据 revision。
- 进程异常退出后，clip/binding journal 能恢复到最后一个完整记录。
- 不删除原始 TS、archive 或已确认关联引用的文件。

### 2. 极限压力指标

- 测试输入：600 条以上历史、双机位、5 秒内 50 人、至少两轮全量 revision。
- 50 人 5 秒冲刺每个机位最多生成 1 个正常共享片段；跨 20 秒上限时允许按规则分割。
- 本次 1327 行样本的新模型共享片段目标不超过 50 个，正常预期约 42 个。
- 正常直接绑定查询不调用全量 `locate_passage()`。
- 首次刷新文件可播放性检查不超过不同活动 clip 路径数的常数倍。
- UI 主线程单次阻塞目标低于 100 ms，最大不得持续超过 200 ms。

### 3. 判读交互指标

- 同一共享片段内切换运动员，首帧显示 p95 不超过 150 ms。
- 跨共享片段切换，SSD 上首帧 p95 不超过 500 ms。
- 缓存命中的单帧前后移动 p95 不超过 50 ms。
- 缓存未命中的单活动机位精确帧 p95 不超过 400 ms。
- 非活动机位不能因连续方向键产生同等数量的 seek。
- 并排窗口连续切换 100 次后，worker 数、线程数和缓存字节数回到稳定区间。

### 4. 资源指标

- 16 GB 笔记本中 FinishReview 私有内存保持在 1 GB 以内，连续操作不持续增长。
- 8 GB 设备不依赖关闭 Defender、增加 NVR 或购买独立显卡完成基本判读。
- 录像和判读同时运行时，派生清单发布不能长时间占满 UI 线程或磁盘队列。
- 打包 EXE 与源码运行的 clip/binding 路径解析结果一致。

## 十五、风险、兼容和回退

1. **区间分割风险**：20 秒只是资源上限，不得截断任何事件的 pre/post roll；必要时允许相邻 clip 重叠。
2. **晚到和乱序风险**：已封口 clip 不修改；晚到事件优先绑定覆盖它的旧 clip，否则创建新 clip。
3. **证据引用风险**：清理必须按 clip 引用计数、活动 binding 和确认关联共同判断，不能按文件时间直接删除。
4. **并发风险**：派生文件移出 UI 线程后必须使用单写入队列，避免 playlist、timeline 和 binding 顺序不一致。
5. **兼容风险**：当前没有正式比赛数据，新录像可以直接使用新模型；现有测试赛事继续通过旧 timeline fallback 打开，不做破坏性迁移。
6. **播放器风险**：共享片段能减少重开和重复 seek，但不能让压缩视频的首次反向缓存未命中变成零成本。
7. **回退策略**：阶段 B、C、D 分开提交；保留 legacy publisher 和 `locate_passage()` fallback，现场验收前不删除旧路径。

## 十六、最终执行顺序

1. 阶段 A：建立 663 人固定压力样本和可重复性能基线。
2. 阶段 B：完成路径索引、时间索引、文件缓存和范围刷新。
3. 阶段 C：完成共享 `ReviewClip`、活动 binding 和 revision 审计语义。
4. 阶段 D：完成同片段播放器复用和双机位并排判读。
5. 阶段 E：在 16 GB SSD 和 8 GB 现场笔记本验收。
6. 达标后再决定是否需要 FFmpeg 关键帧后端或 mpv 原型。

当前正式决策是不修改 CycleRace 过线语义、不增加 NVR、不先更换电脑、不直接接入新的媒体服务器，也不删除现有测试数据。长期优化的主体是共享冲刺片段、事件直接绑定、播放器复用和单活动机位判读。
