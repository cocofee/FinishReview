# v0.24.8 之后的单视频架构实施方案

编写日期：2026-09-18。已复制到 issue-4 工作区实施 A+B+C；本方案保留后续路线，实际完成情况见 [.field_validation/issue4-2026-09-18.md](.field_validation/issue4-2026-09-18.md)。

基线：`main` / `v0.24.8`，提交 `7d215d0c6503a5ddae792deda1d651d9dd9fc23d`。

依据：`ISSUE_3_ARCHITECTURE_OPTIMIZATION_PLAN.md` 第 9 节、当前源码和本次发布验证。旧架构文档里的历史问题不能直接当作待办。当前阶段是原计划“阶段 4：收拢架构与现场验收”的继续实施，并处理代码核对时发现的剩余 UI 同步 I/O。

## 1. 目标、范围与发布边界

本轮目标：单视频边录边判时，接收落盘、赛事切换和后台恢复有明确所有者；界面只接收已提交结果，操作员正在查看的原帧与身份不会被后台刷新改变。

- 当前仅优化单视频模式。已有其他机位及 RaceTiger 路径维持兼容性回归，不增加双路能力。
- 时间胶卷用于查看有没有人通过；机位 1 判读窗口暂停后加载原始分辨率，逐帧后更新高清。
- 保留 Python、PyQt5、FFmpeg、OpenCV、JSONL、共享 ReviewClip 和事件 revision 绑定。
- 比赛使用已发布的 v0.24.8；新工作区从 main 建独立开发分支。完成回归及现场验证后再生成下一候选版，不覆盖比赛 EXE 或正式赛事数据。
- 暂缓双机位专项、独立视觉检测改造、OCR、自动判定、数据库迁移、硬解和解码后端替换。
- 不把主窗口行数作为完成标准；以所有权、依赖边界和实际响应改善验收。

## 2. 已完成的能力

| 能力 | 当前实现 | 后续处理 |
|---|---|---|
| 后台证据流水线 | capture_refresh.py、evidence_pipeline.py | 继续使用现有串行所有者，不重建另一套发布系统 |
| 共享片段与 revision 绑定 | review_clip.py、review_recorder.py | 保留幂等、撤回、审计和旧时间线 fallback |
| 增量名单与录像目录 | passage_review.py、recording_catalog.py、video_timeline.py | 提取边界，保留已有增量更新收益 |
| 解码资源管理 | decode_resources.py、video_playback.py、race_filmstrip.py | 保留优先级、取消、缓存和 capture 预算 |
| 录制恢复 | recording_controller.py | 会话协调复用其启动、停止、回滚和重连 |
| 接收器生命周期 | receiver_controller.py | 扩展与会话边界的协作，不重写 HTTP 接收器 |
| 状态展示与设置对话框 | runtime_status.py、launch_dialog.py | 已完成拆分，不重复列为新增任务 |
| 机位 1 暂停高清 | passage_review.py、video_playback.py | 固定原帧、尺寸、失败重试和释放语义 |
| 本次录像范围与胶卷时钟 | race_filmstrip.py、review_window.py | 保留开始录像边界、重连保持和录像北京时间 |
| HLS 暂时文件锁 | review_recorder.py | 保留两次有界重试，持续失败不能清空已有索引和保护 |
| 配置容错、HTTP 基础健壮性、隔离构建 | 已有代码和测试 | 不按旧路线图重新实施 |

发布验证基线：818 passed、1 skipped；Ruff、compileall、pip check、发行目录检查、目录版与单文件 EXE 独立启动通过。这是已有结果，后续变更须重新运行相应检查。

## 3. 当前真实缺口

以下行号对应基线提交，实施时以符号名为准。

| 缺口 | 代码依据 | 影响与优先级 |
|---|---|---|
| UI 回调仍向当前赛事日志同步追加 | review_window.py 的 _on_passage_received()，约 3095 行；PassageEventStore.append() 含 fsync | CycleRace 先写收件日志，但投影到赛事日志仍可能阻塞 UI。P1 优先 |
| 赛事准备和合并仍同步执行 | _apply_settings() 约 1524 行；_activate_cyclerace_workspace() 约 2921 行；_merge_cyclerace_events() 约 2969 行 | 多日志加载、目录访问、metadata 保存和历史 revision 合并仍在窗口路径。P1 |
| 切换屏障直接等待后台条件变量 | _apply_settings() 调用 invalidate_and_wait(timeout=5.0)；capture_refresh.py 约 167 行 | 慢任务下可能阻塞 GUI，需转为可响应的异步状态转换。P1 |
| 会话状态散落在主窗口 | review_window.py 共 4193 行，FinishReviewWindow 有 108 个方法 | 设置、历史/实时切换、接收路由和退出顺序难独立验证。P1/P2 |
| 判读选择控制器仍依赖整个窗口 | review_selection.py 保存 review: Any 并读写私有成员 | 已拆文件但依赖未收拢；不适合独立推理身份与原帧关系。P2 |
| 判读界面承担多种职责 | passage_review.py 共 9553 行，PassageReviewSurface 有 221 个方法 | 名单投影、判读会话、弹窗托管、标记与绘制相互耦合。P2 |
| 时间文档与新行为不同步 | README 仍有“胶卷、机位和记录统一显示校时后的判读时间” | 与已发布的录像北京时间刻度冲突，需要同步说明。P1 文档项 |
| 真实设备与长期稳定性证据不足 | .field_validation/issue3-2026-09-18.md 待验收项 | 本地自动测试不能替代完整赛事、长时间 RTSP 与故障恢复。发布门槛 |
| 单文件发布过程尚未全部纳入正式脚本 | build.ps1 默认构建目录版；v0.24.8 单文件 spec/校验助手在本地 artifacts | 后续发布应可在新工作区重复执行。P2 工程项 |

上述同步路径由源码核对确认；尚未测量现场磁盘下的实际阻塞时长，不把推测写成实测结论。

## 4. 目标结构与所有权

```text
CycleRace / RaceTiger
  -> 接收线程：原始过线事件持久化成功
  -> 事件路由与赛事日志投影：有序提交、恢复、revision 去重
  -> 已提交事件变化 -> GUI 名单增量投影
                    -> 现有 EvidencePipeline
                         -> pin -> 清单 -> timeline -> binding -> cleanup
                         -> 已提交只读快照 -> GUI 局部更新

SessionController
  -> 准备/切换/关闭赛事、协调 receiver 与 recording controller
  -> SessionContext + generation

ReviewSessionState
  -> 当前身份、事件 revision、媒体原帧、未确认标线、胶卷浏览位置
  -> 播放/逐帧/定位/高清请求
  -> 现有解码调度和资源池

FinishReviewWindow / PassageReviewSurface
  -> 控件组装、用户输入、信号绑定、渲染状态
```

所有权必须明确：

1. 接收器仍在持久化成功后返回成功和通知，不把 HTTP 确认改成仅进入内存队列。
2. CycleRace 收件日志归接收器；当前赛事日志投影归明确的单写入路径。RaceTiger 已直接写入对应 store 的路径不得再无条件追加一遍。
3. 事件投影与证据任务是不同职责。慢扫描、pin、媒体发布不能堵住名单事件落盘和提交通知。
4. 证据发布和清理仍归现有串行队列，保持“保护媒体 → 原子清单 → timeline → clip/binding → 释放临时保护”。
5. 会话 generation 标识数据源上下文；事件 revision 标识事件版本；播放 request generation 标识帧请求。三者不能互相替代。
6. 可失效的是旧 UI 结果；已接收的持久化、撤回与证据保护任务不能因为切换界面而被静默丢弃。
7. 控制器不保存整个主窗口或任意 QWidget；明确传入只读数据、服务和少量回调。Qt 控件仅由 GUI 线程修改。
8. 操作员选择不归后台证据流水线所有；后台录像就绪不能自动换号码、跳帧、移动胶卷或确认判读。

## 5. 阶段 A：固化接手基线与可复现样本

目的：为后续两个关键链路建立差分证据，不重复实现已完成的优化。

工作：

- 从 main 创建 `refactor/single-video-session` 等独立分支，记录基线 SHA 和依赖版本。
- 阅读本方案、原 Issue #3 第 9 节及现场记录；现有测试中已覆盖的行为直接复用。
- 先保留一份无正式赛事数据的工作目录，所有故障注入、退出恢复与压力测试使用测试副本。
- 使用现有 tools/benchmark_review.py、benchmark_video_playback.py、benchmark_decode_concurrency.py，补测“赛事日志慢写”和“慢赛事切换”两个缺口。
- 记录三个独立时刻：接收 durable commit、赛事投影 commit、名单可见；本进程耗时用单调时钟，不能受录像/判读校时影响。
- 每份结果带构建 SHA、硬件、媒体分辨率/编码/GOP、是否真实录制、是否实际绘制、OS 缓存条件和失败数。
- 修正文档的时间语义；保留历史验证记录，不把旧实验数据覆盖成新结果。

交付：基线报告、必要的边界行为测试、后续比较样本。

完成标准：能复现慢 I/O 对这两个入口的影响，明确已有测试覆盖与新增缺口。性能目标未达到不能通过修改基准媒体或关闭关键功能掩盖。

## 6. 阶段 B：事件接收投影退出 UI 线程（下一工作区第一批生产代码）

建议新增 `realtime/event_ingestion.py`，模块名可在实施时微调。

职责：接收已持久化通知，选择赛事目标、执行必要的赛事日志追加，返回已提交变化；不操作表格、播放器或证据文件。

具体步骤：

1. 定义不可变上下文标识和结果：赛事/赛段、数据源标识、session generation、event_id、revision、提交状态。保持原有事件去重键的兼容性，不顺便更改 JSONL 格式。
2. 分离“已收到”与“已投影”：网络线程完成原始日志落盘后发通知；赛事投影成功后才更新依赖该赛事 store 的名单与证据请求。
3. 将 _on_passage_received() 的赛事 append、必要的持久化路由移至专用有序后台执行路径。优先采用一个有界单线程投影 worker，避免排在慢证据扫描后面；不为每条事件启动线程。
4. 已经由接收器直接写入同一 store 的来源走已提交通知路径，避免重复写入。收件日志和赛事日志相同/不同、CycleRace/RaceTiger 分别覆盖测试。
5. UI 只应用提交后的变化，继续使用现有 _pending_passages 和 refresh_events() 合并显示；更正和撤回无需等待录像就绪。
6. 队列里的已接受写入按顺序执行，普通事件不使用预览请求的“只保留最后一个”策略。队列达到上限时记录待恢复状态，从已持久化收件记录恢复；不得只打印错误然后丢通知。
7. 恢复策略沿用已有最高 revision 与撤回 tombstone 规则。原始收件日志保留完整审计；若恢复只能重建最新赛事投影，必须明确这一边界，不能宣称补写了每个中间 revision。
8. 同一事件高 revision 到达后，旧提交结果不得回写新 UI。赛事已切换时，旧赛事的持久化任务按生命周期规则完成，旧 UI 结果失效。
9. 写盘失败时保留原始收件数据和待恢复状态，状态栏显示原因；恢复后幂等补齐。没有持久化成功的投影不能显示成已成功准备证据。

验收：

- 注入赛事日志慢写/失败，UI 定时器和用户输入仍可处理，录像继续运行。
- 重发同一 revision 无重复可见行或绑定；更正更新对应事件；撤回不会在切赛事/重启后复活。
- 队列满、应用退出、赛事切换、失败后恢复均有行为测试，能证明持久化事件最终可重建。
- 500/2000/5000 人样本中，单条更新仍走增量路径；慢证据任务不会增加名单提交等待。
- 对已有正常路径做同机前后比较，记录新增投影队列的延迟和线程成本。

提交建议：先提交上下文与边界测试，再提交 worker 与 UI 接线，再提交积压/失败恢复；每一步保持可运行。

## 7. 阶段 C：赛事会话准备、切换与退出

建议新增 `realtime/session_context.py`、`realtime/session_controller.py`，复用 event_workspace.py、ReceiverController、RecordingSessionController 和 CaptureRefreshWorker。

SessionContext 包含当前数据源、赛事目录、store/service 句柄与 generation。后台准备对象可包含服务引用，但传给 UI 的结果必须有明确不可变快照；不能把 mutable store 当作只读快照。

拟定转换：`active -> preparing -> draining -> activating -> active`；另有 `closing` 与明确的失败结果。状态名称不是新增用户界面文案。

实施顺序：

1. 抽出工作目录校验、store 加载、metadata 保存和历史 revision 合并，消除 _apply_settings() 里位置相关的大 tuple。
2. 目标赛事在后台准备，准备失败仍保留旧会话。准备过程中到达的新事件先可靠存入接收日志，激活时补齐到明确水位，激活后按顺序继续消费。
3. 用有上下文的 Future/完成信号表达后台屏障；GUI 不直接执行条件变量等待或 thread.join。可响应等待仍需阻止重入的切赛事、启动录像等冲突操作。
4. 不将 invalidation 等同于 durable 队列排空：必须覆盖已接受显式任务、待提交事件、最终归档与保护关系，不能只等一个 _active_generation 字段。
5. 等旧会话处置结果明确后再切换 UI 绑定和 generation；prepare 成功不代表旧录像进程已经停止。
6. 失败分两类：切换前失败保留旧会话；旧录像已安全停止后失败，保留旧数据和明确“已停止/切换失败”的实际状态，不虚报已恢复录像。
7. 保留历史赛事查看期间继续接收实时数据、返回实时后合并最高 revision 与撤回的现有行为。
8. 将停止/退出顺序集中编排：停止接收新操作、处置接收与投影水位、排空必要任务、完成录像收尾与保护、释放解码/扫描器、关闭窗口。实际调用顺序以依赖和回归验证确定。
9. 在状态切换里使用已有录制 controller，不复制单机位重连和停止失败保留逻辑。

验收：

- 慢目录/慢日志/后台任务期间打开历史赛事、返回实时仍有界面响应。
- metadata 晚到、切换中更正/撤回、旧高清帧迟到、旧扫描结果迟到均不污染新赛事。
- 打开失败、停止失败、设置保存失败各自保留一致的运行状态；仍运行进程的引用和保护不能丢失。
- 新一次人工开始录像建立“本次录像”边界，自动重连保持原边界，停止后可回看，切数据源按现有规则重置。
- 没有开枪/重置协议时，不将任意 metadata 更新、首条过线或重新连接推断为“新一枪”。

阶段 B、C 是下一工作区的首个里程碑。全部通过后再进入大判读窗口拆分。

## 8. 阶段 D：判读状态和名单投影分离

issue-4 首批 D 边界已开始实施：`review_session.py` 保存无界面判读身份、原帧、胶卷位置、
高清帧和请求 generation；`roster_projection.py` 保存名单顺序、筛选、状态和局部变化集合。
当前 QTableWidget 仍是显示控件，后续接线必须继续保持已有增量更新和人工操作语义。

这是后续里程碑，不与接收/会话改造一次性混合。

建议按依赖顺序逐块抽取：

| 模块 | 职责 | 明确不承担的职责 |
|---|---|---|
| review_session.py | 当前身份/revision、媒体帧、未确认标线、浏览与判读模式；产生操作计划 | 不写证据日志，不操作 QWidget |
| review_selection.py | 从只读选择上下文生成选择计划 | 不保存整个 review 窗口，不直接改按钮 |
| roster_projection.py | event_id 索引、筛选状态、行变化集合、针对性失效 | 不创建播放器，不全场扫描录像 |
| 现有 PassageEvidencePane | 当前机位显示和播放控件 | 不接管赛事生命周期 |
| 现有 view/surface | 将计划应用到控件、托管同一个机位窗格和名单 | 不自行决定事件持久化/撤回语义 |

关键规则：

- 单击名单换身份且保持当前原帧；双击定位芯片时间；胶卷点击定位原帧但不自动换号码。
- F 打开机位 1 判读，Esc/F 返回后保留胶卷浏览位置；移动复用原窗格，不创建第二套播放器。
- 连续播放采用预览；判读窗口暂停才请求高清，逐帧后加载对应原图；关闭判读取消高清并释放大图。
- 高清替换不改变帧号、媒体位置、标线坐标、缩放和确认关联；过期请求不得覆盖当前帧。
- 胶卷浏览/缩略图加载不等同人工检查；选择身份、自动刷新不生成判读确认。
- 明确处理当前事件撤回和 revision 变更，遵守已有失效规则，不能继续确认失效上下文。
- 保留已有 QTableWidget 增量实现。仅当固定 5000 人场景测量仍超标，才另立 QAbstractTableModel 迁移方案。

验收使用现有行为回归，加上不依赖 QWidget 的状态转换测试；不写只验证“方法被搬到新文件”的测试。持续打开/关闭 100 次，资源回到稳定范围。

## 9. 阶段 E：时间与录像会话边界收口

复用 time_domain.py 的时间类型，优先补齐新模块的接口，不另造一套时间模型。

| 时间/边界 | 用途 | 约束 |
|---|---|---|
| 录像绝对时间 | 时间胶卷刻度、本次录像范围、归档时间范围 | 按北京时间显示，不减首人判读校准偏移 |
| 芯片/正式计时时间 | 定位、正式判读业务输入 | 延续当前协议和跨天对齐规则 |
| 校时后的判读时间 | 判读关联、相应记录及明确标注的提示 | 转换只在明确边界发生，不反向污染录像刻度 |
| 媒体位置与原帧号 | seek、逐帧、标线、保存原帧 | 不与 wall-clock 或偏移直接混用 |
| 开始录像边界 | “本次录像”视图 | 不冒充开枪事件；重连保持，主动重新开始重建 |

先补现有行为测试和 README，再随阶段 C/D 抽取逻辑；不为整理类型改变已经保存的校准或证据关联。覆盖跨天、异常大偏移、历史赛事、重连、重新开始、原帧回看。

## 10. 阶段 F：真实性能、长期运行与发布复现

先跑真实主窗口日常链路：一个录像源 + 接收 + 名单筛选 + 胶卷浏览 + 机位 1 逐帧/暂停高清。活动分析和检测不在默认主流程中额外启用；额外压力样本单列。

现有性能结果和边界：

- H.264 1080p25 倒放约 20.0–20.7 fps；H.265 1440p25 约 17.8–18.9 fps。来自 Qt offscreen 绘制样本，未达到源视频 25 fps。
- H.265 额外并发压力私有内存约 1.3 GiB，超过 1 GiB 目标；不能把图像缓存预算当作整个进程内存。
- TS/HLS 保守顺序解码的倒放不属于上述性能改善范围。

验证安排：

1. 每个阶段执行针对性行为测试和 Ruff；里程碑完成后全量 pytest、compileall、pip check。
2. 真实单路素材先做 15–30 分钟边录边判，再做至少 4 小时持续运行。记录峰值和趋势，不能只记录开始/结束两个点。
3. RTSP 断连恢复、磁盘/文件锁、目录不可达、重复/更正/撤回、停止重启使用测试赛事执行；USB/共享目录仅在实际使用路径需要时验证。
4. 准确测量名单更新、GUI 事件循环、首帧/高清呈现、实际绘制帧号、私有内存、子进程占用、capture、线程、任务积压与资源释放。
5. 同硬件/媒体对照，记录 p50/p95/max 和失败数；性能恶化先定位原因，不修改目标来通过验收。

沿用原架构目标（目标，不是已有达标承诺）：

| 项目 | 验收目标 |
|---|---|
| GUI 延迟 | p95 < 100 ms；持续 > 200 ms 停顿须定位并解决 |
| 已持久化事件到名单可见 | 正常存储条件下 p95 ≤ 250 ms；故障积压另报恢复时间 |
| 5000 人单条增量应用 / 搜索 | p95 < 100 ms / ≤ 150 ms |
| 同共享片段首帧 / 跨片段首帧 | p95 ≤ 150 ms / SSD 上 ≤ 500 ms |
| 缓存命中逐帧 / 未命中精确帧 | p95 ≤ 50 ms / ≤ 400 ms |
| 暂停高清 | 原始像素与帧号正确；读取耗时单列，不冒用预览帧指标 |
| 资源 | 正常配置私有内存以 ≤ 1 GiB 为目标；线程/capture/缓存/队列有界，长时间不持续增长 |
| 数据正确性 | 无错误赛事污染、撤回复活、证据误删、错误原帧确认 |

只有真实素材仍不达标且瓶颈已定位到解码后端时，才做 FFmpeg 关键帧/硬解独立 A/B 原型；先验证原帧精度、取消、内存与发布依赖，再决定是否替换 OpenCV。

发布复现工作：

- 将本次本地单文件构建流程整理进 packaging，统一目录版与单文件版的依赖、入口和版本资源。
- 自动核对 pyproject.toml、realtime/__init__.py、Windows FileVersion/ProductVersion 的字符串和数值版本。
- 正式发布时从同一提交构建，生成 SHA256 与 build-info，记录 Python/PyInstaller/FFmpeg 版本。
- 对目录版 ZIP 解压副本和独立 EXE 做隔离目录启动验证；清理环境变量影响，使用测试 LOCALAPPDATA，等待实际退出码与 smoke 日志。
- 确保包内没有赛事数据、日志、本机配置和非项目依赖。涉及生命周期/打包改动时必须验证 EXE。
- 下一版本号在实际发布时确定；开发中不提前覆盖 v0.24.8 标签或桌面正式包。

## 11. 提交与里程碑划分

| 顺序 | 建议提交/工作包 | 完成条件 |
|---|---|---|
| A | baseline: 固化事件投影、赛事切换和时间语义 | 行为与测量路径明确，文档一致 |
| B1 | refactor: 显式会话身份和事件提交结果 | 新接口不引用窗口，保持原行为 |
| B2 | perf: 赛事事件投影移出 UI | 慢写时 UI 可响应，名单增量保持 |
| B3 | fix: 积压/失败/退出恢复 | durable 数据可恢复，撤回不复活 |
| C1 | refactor: 后台准备赛事上下文 | 加载/合并离开 UI，失败保留旧会话 |
| C2 | refactor: 异步切换和退出编排 | 屏障、generation、停止失败语义完整 |
| D | refactor: 判读会话和名单投影 | 操作员语义不变，控制器依赖收拢 |
| E | refactor/docs: 时间与录像范围边界 | 时间转换可追踪，历史关联兼容 |
| F | build/validation: 可重复发布与现场验收 | 同提交双产物、测试和真实运行记录完整 |

首个里程碑只承诺完成 A+B+C。每个提交可独立验证，失败可回退；不得将数据模型迁移、解码替换与会话拆分混成一个大提交。

## 12. 新工作区接手指令

可将下面内容直接发给新工作区：

> 继续 FinishReview 单视频架构优化。基线是 main/v0.24.8，发布提交 7d215d0c6503a5ddae792deda1d651d9dd9fc23d。先核对当前 main 是否有更新；保留用户已有改动。阅读 NEXT_ARCHITECTURE_PLAN.md、ISSUE_3_ARCHITECTURE_OPTIMIZATION_PLAN.md 第 9 节及 .field_validation/issue3-2026-09-18.md，不要重复实现已完成的后台证据队列、增量索引、解码资源池或暂停高清。当前只做单视频，胶卷只看有人通过，高清仅在机位 1 判读暂停后读取。先完成方案 A+B+C：补齐接收/切赛事边界验证，将当前赛事日志投影移出 UI，再抽取赛事上下文、异步切换和退出编排。沿用现有 recording/receiver/evidence 控制器，不重写播放器、不改 JSONL 格式、不扩展双机位。分步实现并验证，完成首个里程碑后报告改动、测试、性能对照及未验收项。比赛使用的 v0.24.8 EXE 和正式赛事数据保持独立。后续判读状态拆分和发布工具整理按本方案继续安排。

本文原始版本来自旧工作区：

`C:/Users/联想/orca/workspaces/FinishReview/issue-3/NEXT_ARCHITECTURE_PLAN.md`

issue-4 的实现、回归、性能对照和待验收项单独记录；没有发布新版本或覆盖比赛使用的 v0.24.8。
