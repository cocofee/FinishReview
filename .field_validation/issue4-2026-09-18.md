# Issue #4 首个里程碑：A+B+C

日期：2026-09-18。基线 main / v0.24.8：`7d215d0c6503a5ddae792deda1d651d9dd9fc23d`。
接手时工作区无用户修改，origin/main 与该提交一致。方案由 issue-3 复制到根目录
`NEXT_ARCHITECTURE_PLAN.md`。本轮在 issue-4 独立工作区开发，没有发布、覆盖比赛 EXE
或访问正式赛事数据；当前产物为未发布的开发验证包。

## 实现与边界

- `event_ingestion.py`：接收已持久化通知，一个串行线程处理赛事投影。输入队列及提交
  通知各最多 256 项；溢出/失败从收件日志恢复。普通队列保留顺序，恢复合并最高
  revision，包括撤回 tombstone；中间审计版本保留在收件日志，不宣称恢复补写每个版本。
- `PassageEventStore`、`RaceMetadataStore`：读锁与写盘锁分离，fsync 完成后才发布内存
  状态。读操作不再等慢写盘。新增单调时钟只用于进程内度量，不进入 JSONL 协议。
- UI 提交回调先增量更新名单，再触发现有证据队列。录像准备不计入名单延迟。
  RaceTiger / 同一日志路径不重复追加。退出时通知邮箱可以淘汰旧 UI 通知，写盘仍排空，
  不依赖 Qt 继续消费通知才能退出；磁盘持续失败时收件日志用于重启恢复。
- `session_context.py`：赛事服务引用与不可变加载快照分别保存。准备、日志加载、
  revision 合并、metadata 写盘进入后台。打开历史赛事也补齐收件日志里的撤回。
- `session_controller.py`：单线程准备与生命周期任务，显式 transition 防重入，管理
  generation、事件排空、证据 FIFO 屏障及关闭。沿用 receiver/recording/evidence 控制器。
  切换前先封住旧投影写入器，避免同一 JSONL 同时存在两个写入所有者；后台准备完成后
  排空旧证据、激活上下文并重放准备期间的收件记录。
- 目标准备失败保留旧服务；接收器或录像进程停止失败时保留引用，不激活新赛事。
  录像已停止后的失败不虚报录像恢复。旧赛事提交、扫描和原帧结果沿用各自 generation
  及播放器取消机制；切换清除旧选择和回放窗口。
  额外覆盖“写盘已提交、Qt 通知尚未投递”时切换失败：根据未呈现 revision 补齐旧名单，
  避免重绑投影器后把该记录误认为已经显示。修复前测试稳定失败，修复后通过。
- 显式切换仍提供同步返回值，内部使用 Future + Qt 事件循环等待，期间禁用冲突操作，
  保持绘制、定时器和信号分发；它不是可同时执行多个用户切换操作的模式。
- 回归暴露的回放退出问题已修复：关闭后的延迟错误不弹窗，重复关闭不再访问已删除
  worker。保留单击换身份/原帧、双击定位、胶卷观察、机位 1 暂停高清及人工确认规则。
- README 胶卷时钟改为录像北京时间。未进入判读状态拆分、表格模型迁移、硬解或双机位专项。

## 自动回归

原始基线：818 passed、1 skipped。最终全量回归：**841 passed、1 skipped，174.35 s**，
日志 `artifacts/issue4/pytest-delivery-final.txt`。会话子集 11 passed，日志
`artifacts/issue4/pytest-undelivered-fixed.txt`；修复前失败证据保留在
`artifacts/issue4/pytest-undelivered-before.txt`。

新增 23 项覆盖：顺序与去重、队列满恢复、持续/暂时磁盘失败、重启撤回、同日志无二次写、
慢 fsync 读取、通知有界及无 UI 消费退出；慢投影、慢历史加载、显式证据屏障、准备失败、
切换中撤回、旧结果丢弃、退出重入、录像/接收器停止失败、历史撤回恢复、未投递提交及回放迟到错误。
原有主窗口测试只适配异步提交等待，保留业务断言；批处理测试排除等待时正常的周期扫描。
Ruff、compileall、pip check、git diff --check 通过。

测试命令（本机 PyQt5 中文安装路径需要显式插件路径）：

```powershell
$env:QT_QPA_PLATFORM = 'offscreen'
$env:QT_QPA_PLATFORM_PLUGIN_PATH = "$PWD/.venv/Lib/site-packages/PyQt5/Qt5/plugins/platforms"
$env:QT_QPA_FONTDIR = 'C:/Windows/Fonts'
.venv/Scripts/python.exe -m pytest -q
```

## 性能对照方法

`tools/benchmark_session_io.py` 使用真实主窗口、临时 JSONL、Qt offscreen 绘制，不含录像
或解码。分别测接收持久化完成 → 赛事提交完成 → 名单可见，另测历史加载、返回实时及
三阶段的 10 ms GUI 定时器间隔。慢场景给赛事 append、日志加载、已接受后台任务各注入
200 ms；正常场景无注入。每种 10 次，OS 缓存未清空，p95 仅作同机回归对照。

基线从 `git archive 7d215d0` 提取，使用同一基准脚本及本工作区 Python 依赖；不建立新
工作树或改写 main。JSON 保存源码 SHA-256，避免把未提交工作树误写成发布提交的实测。
最终测量在构建、pytest 结束后串行运行。实测数值及原始文件见下方追加结果。

## 构建复现

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File packaging/build.ps1 -Mode both -FfmpegPath C:/path/to/ffmpeg.exe
```

`tools/build_release.py` 生成版本资源、目录版 ZIP、单文件 EXE、SHA-256、依赖及源码摘要、
FFmpeg 版本/摘要和 UTC 构建记录。验证实际 EXE 的字符串/数值版本；使用临时 LOCALAPPDATA，
对 ZIP 解压副本和复制后的独立 EXE 分别运行 offscreen、Windows 插件的 `--smoke-test`。
CI 构建入口同步为 `-Mode both`，本地通过不代表已运行远端 CI。

最终构建记录：`artifacts/build-records/20260918T134119193876Z/build.json`，状态 passed。
目录版 SHA-256 为 `c50d1d5cc128293b82eef77736dfe25511efd5f6d35bd4d8ed012ae2e0df8c82`，
单文件版 SHA-256 为 `bfe6fcad4c8c373aceab4c935f1f5bd06e6ce0eb2210849ca3709ec6f77fb57c`；
记录中的源文件和产物摘要复核无差异。

修复了本机 PyQt5 在中文用户目录把路径读成 `C:/Users/??/...` 的问题：spec 在路径失效
时从实际 wheel 修复插件与 Qt DLL 搜索目录，不修改 Python 安装。Analysis 后强制检查
qwindows.dll、qoffscreen.dll，避免插件被过滤后仍产生无法启动的包。失败尝试保留原日志，
不计为通过。版本号仍是开发基线 0.24.8，以构建记录区分；未创建新发布标签。

## 仍待现场验收

- 真实单视频：录像、接收、筛选、胶卷浏览、逐帧和暂停高清并行 15–30 分钟，再连续至少
  4 小时。当前没有连接现场设备，不能用上述合成计时替代 GUI p95 <100 ms 的现场验收。
- 测试赛事下验证断网恢复、实际文件锁、磁盘写失败、停止和重启，采集内存、线程、
  capture、缓存和队列的长期曲线。当前有单元故障注入证据，尚无真实设备长期曲线。
- 同片段/跨片段首帧、缓存命中逐帧及源视频 25 fps 倒放目标沿用旧计划；本轮没有改变
  解码后端，也不宣称这些真实媒体目标已达成。
- 会话准备和退出仍含受保护的显式模态等待；初始构造窗口之前的启动加载保持原路径。
  主窗口仍有 Qt 接线与运行协调，后续 D 阶段再继续收拢判读会话和名单投影。
- 5000 人全量名单重绘仍慢，本轮没有迁移表格模型；历史赛事大量行激活时的 GUI 停顿
  需要额外测量。本轮小赛事 I/O 基准不覆盖这种大表首次构建的成本。

## 最终 I/O 对照

最终串行运行结果：慢写盘 p95 的接收回调从基线 **236.81 ms** 降至 **0.15 ms**，
GUI 间隔从 **462.02 ms** 降至 **16.15 ms**；持久化到名单可见为 **378.97 ms**，其中
录像准备未计入。正常存储下回调 **0.07 ms**、持久化到名单可见 **174.15 ms**。
历史打开/返回实时慢场景为 **300.77 / 315.73 ms**，期间 GUI 间隔 **20.38 / 16.53 ms**；
等待后台任务仍能处理绘制和定时器。原始文件：
`artifacts/issue4/session-delivery-slow.json`、`session-delivery-normal.json`，对应基线文件
以 `session-milestone-baseline-` 开头。指标是临时 JSONL + offscreen Qt，同机差分证据，
不替代真实设备目标。

## 名单增量复测

`tools.benchmark_review --sizes 500 2000 5000 --ui-iterations 10`，合成 MJPG 32×32、10 fps，
Qt offscreen，冷首次构建后重复使用对话框，无实际录制。生产名单实现未修改。
原始数据 `artifacts/issue4/roster-milestone.json`（PowerShell UTF-16 输出）。

| 名单人数 | 单条更新 p95 / ms | 带筛选单条更新 p95 / ms |
|---|---:|---:|
| 500 | 4.205 | 6.869 |
| 2000 | 6.845 | 10.210 |
| 5000 | 60.370 | 62.729 |

5000 人首次构建约 2275 ms，全量刷新 p95 1679 ms，筛选下 100 条批量更新 p95 121 ms。
这些数据支持继续使用单条增量路径；不代表所有名单操作均达到 100 ms。

## D 阶段首批边界

推送 A+B+C 后继续完成了 D 的第一小步：新增 `review_session.py` 和
`roster_projection.py`。前者不拥有 QWidget，明确单击换身份保留原帧、双击定位进入判读、
返回胶卷保留浏览位置并释放高清图，且拒绝旧身份/旧 generation 的迟到帧；后者保存名单
顺序、筛选状态、revision 状态和 added/removed/updated/order-change 集合。现有表格渲染仍
负责显示和行操作，未迁移模型。

该批次新增 8 项纯状态测试；判读界面回归 143 项通过。完整回归将在提交前重新运行。

## 2026-09-19：D 阶段选择上下文

`ReviewSelectionController` 改为通过显式 `ReviewSelectionContext` 读取事件、录像定位、
当前 pane 和选择状态，返回原有选择计划。主窗口负责组装服务、清除临时身份提示及应用
计划；旧位置参数与 `review=` 调用由兼容适配层支持。当前仍读取 pane 的媒体状态，
尚未将播放器接口全部转换成纯数据快照，不把本次改动视为整个 D 阶段完成。

事件仓库和定位字典均在调用时读取，避免刷新重建字典或赛事激活更换 store 后继续使用
旧对象。继续保持单击换身份保留原帧、双击优先连续录像定位，以及原有暂停高清和人工
确认流程。没有改动录像、JSONL、表格模型或解码调度。

新增 6 项测试覆盖显式服务独立运行、容器替换后的 revision/定位读取、当前 pane 更新、
连续录像定位、缺失事件或 lookup 时刷新、旧关键字调用兼容及真实界面 store 重绑。
此前定向选择/判读回归 146 项通过；最终全量回归 **855 passed、1 skipped，160.79 s**。
Ruff、compileall、pip check、git diff --check 通过。测试沿用上文 Qt 插件及字体环境设置。
本地完整日志：`artifacts/issue4/pytest-selection-context.txt`，JUnit 结果：
`artifacts/issue4/pytest-selection-context.xml`。此前中途退出且无完整结果的运行不计为通过。

本次为源码边界拆分，未重新打包或发布；既有构建和性能数据仍对应其原始源码版本。
真实设备 15–30 分钟联调及至少 4 小时验收仍待执行。

## 2026-09-19：随包 FFmpeg 被失效环境变量遮蔽

用户运行新 EXE 后仍报告“未找到 FFmpeg”。复现确认本机用户及进程环境变量
`FINISH_REVIEW_FFMPEG` 指向不存在的 `D:/orca/Tools/ffmpeg/ffmpeg.exe`。
旧查找函数在配置路径失效时立即返回 None，未继续检查随包资源；`5c10571` 增加
`_internal` 候选和启动时解析仍未消除这个提前返回。之前仅检查包内二进制及清理后的
启动环境，不能证明真实环境下录制入口能找到 FFmpeg。

修复提交 `2f4efd0`：有效环境变量仍优先，失效路径则继续查找 EXE 目录、随包资源和
系统 PATH。启动日志记录实际解析路径。未修改本机用户或系统环境变量。

正式构建脚本现在对目录版和单文件版各执行四组 EXE 自检：Qt offscreen / windows，
环境变量未设置 / 指向不存在文件，所有组均清空 PATH。自检在 EXE 内使用生产查找
函数，并要求返回随包路径，然后运行 FFmpeg 生成两秒合成 MKV 和 HLS，再解码读取。
使用隔离 LOCALAPPDATA 和临时输出目录，不读取正式赛事配置或连接现场摄像头。

新增失效环境变量测试在修复前 3 项失败，修复后通过；另覆盖有效覆盖路径优先、
录制器启动时发现随包 FFmpeg、自检拒绝缺失或外部 FFmpeg。相关子集 93 passed；
最终全量 **863 passed、1 skipped，161.76 s**。Ruff、compileall、pip check 通过。
本地日志 `artifacts/issue4/pytest-ffmpeg-env-before.txt`、`pytest-ffmpeg-env-after.txt`、
`pytest-ffmpeg-final.txt` 和 `pytest-ffmpeg-final.xml`。

构建记录 `artifacts/build-records/20260919T010156544474Z/build.json`，状态 passed，
源码为干净提交 `2f4efd0`。全部源码及包文件摘要复核一致，两个包的八组自检全部通过。
单文件另以本机原有失效环境变量复验，清空 PATH、Windows Qt、隔离运行，退出码 0；
结果和日志为同目录的 `inherited-environment-smoke.json`、`inherited-environment-smoke.log`。

- 目录版 EXE SHA-256：`7a7d4cbd25ab8c7f57c278cce44a44fc4a43dbbeebe5c898b46ad84f7ec06a1c`。
- 单文件 EXE SHA-256：`3e088761f64663b96950a43f566d5116b2cb83c4859f13296c4677b8dceea33a`。
- 单文件交付副本：`artifacts/dist/onefile/FinishReviewConsole-issue4-2f4efd0.exe`，与构建
  产物字节相同，文件名包含提交号以区分先前候选包。目录版 ZIP 仍在 `artifacts/dist`。

包版本沿用开发基线 0.24.8；本记录为构建后补充文档，不改变包内源码。上述验证覆盖
FFmpeg 发现及合成媒体处理，不替代真实设备录像流程及四小时现场验收。
