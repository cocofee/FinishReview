# Issue #3 代码验证与待验收记录

代码：`4f2d4a5`、`29fd197`、`734af40`；详细进度见根目录 `ISSUE_3_ARCHITECTURE_OPTIMIZATION_PLAN.md` 第 9 节。

## 已完成的本地验证

- Windows 11，i5-13500H，16 逻辑 CPU，约 16 GB 内存，Python 3.12.10。
- 全量 pytest：770 passed、1 skipped；后续资源压力和缓存竞争改动的播放/胶卷子集：102 passed。
- Ruff、compileall、发行目录边界检查、EXE `--smoke-test`。
- 使用合成 H.264 1080p25、H.265 1440p25，验证反向 20 帧序列连续，无跳帧、重复、乱序；H.265 仍未达到源视频帧率。
- 测试覆盖写盘失败重试、慢后台任务时 Qt 事件仍被分发、上下文回调延后、队列停止后排空已接受任务、增量目录失效与恢复、解码取消和共享预算释放。

本地原始日志：`artifacts/issue3/pytest-final3.txt`、`video-benchmark-final*.json`、`build-final.txt`。这些目录不进入 Git。

## 待现场执行

- [ ] 记录真实摄像机型号、编码/GOP、共享目录、CycleRace 版本及构建 SHA。
- [ ] 单/双机位持续录像和接收，操作筛选、定位、逐帧及反向播放至少 15–30 分钟。
- [ ] 数小时运行，记录进程私有内存、GUI 延迟、接收至名单可见、首帧耗时、capture 和缓存峰值。
- [ ] RTSP 断连/恢复、USB 拔插、共享目录不可达、磁盘故障；确认已持久化事件和媒体保护不丢失。
- [ ] 验证录像停止、赛事切换、退出和重启后的证据绑定、手工判读与旧赛事数据。
- [ ] 验证独立视觉检测/活动分析同时启用时的额外资源；这些 capture 尚未纳入判读资源池。

当前没有连接现场硬件，以上项目均未宣称通过。
