# Issue #3：系统架构优化方案

分析日期：2026-09-18。基于 `3061c4d`，版本 `0.24.7`。

需求来源：https://github.com/cocofee/FinishReview/issues/3 。Issue 标题为“对系统架构进行优化”，正文为“主要是速度，性能优化，还有易用性”，没有进一步限定场景。

第 1–8 节保留实施前的分析与方案，代码实施及验收状态见第 9 节。历史行号对应分析时版本。

## 1. 结论与范围

建议保留 Python、PyQt5、FFmpeg 录像、OpenCV 回放和赛事 JSONL 存储，先优化工作在什么线程执行、一次变化触发多少计算，以及哪些解码任务可以同时运行。

优先顺序：修正性能基准 → 录像派生工作退出界面线程 → 名单与录像索引增量更新 → 胶卷与精确判读协调调度 → 沿这些边界拆分界面职责。

首轮以单/双机位、持续接收过线事件、边录边判和整场胶卷浏览为主要场景。8 GB 笔记本、SSD 作为主要验收环境，H.264 1080p/25 fps 作为基础媒体，H.265/1440p 作为压力场景；这些沿用仓库已有性能方案，最终以实际现场配置校准。

## 2. 当前架构及已经完成的优化

当前主要链路为：

```text
CycleRace / RaceTiger → PassageEventStore 持久化 → Qt 回调 → 名单/录像窗口更新
摄像机 → FFmpeg → 连续归档 + HLS 缓冲 → Timeline / ReviewClip / Binding
操作员 → PassageReviewSurface → 播放 worker / 胶卷 worker → 画面与人工确认
```

以下能力已存在，应继续使用，不能作为本次“新增”的收益：

| 已有能力 | 当前代码依据 |
|---|---|
| 共享 ReviewClip、按事件 revision 和机位直接绑定 | `realtime/review_clip.py:86`、`realtime/review_recorder.py:2251` |
| 时间区间索引、路径索引、短期可播放性缓存 | `realtime/video_timeline.py:126`、`:543`、`:559` |
| 同片段 worker 复用、请求 generation、64 MiB 播放缓存 | `realtime/passage_review.py:1896`、`realtime/video_playback.py:393` |
| 可见区域胶卷解码、固定缓存上限、浏览位置保护 | `realtime/race_filmstrip.py:739`、`:1064` |
| 周期后台扫描、请求合并与旧上下文失效 | `realtime/capture_refresh.py:48` |
| 录制/接收/选择控制器、时间类型、线程退出工具 | `recording_controller.py`、`receiver_controller.py`、`review_selection.py`、`time_domain.py`、`thread_lifecycle.py` |
| 性能工具、运行指标、CI lint/coverage、现场验收清单 | `tools/benchmark_*.py`、`runtime_metrics.py`、`.github/workflows/ci.yml`、`.field_validation/README.md` |

`ARCHITECTURE_ROADMAP_REVIEW.md`、`PLAYBACK_PERFORMANCE_OPTIMIZATION_PLAN.md` 含历史分析和后续实施记录；旧问题描述不能直接当作当前缺陷。当前整场可见区域胶卷已取代原先两分钟窗口的主浏览流程，不应为落实旧文档重新引入整窗等待。

## 3. 已核实的主要问题

### A. 性能基准没有维护新增的索引

`tools/benchmark_review.py:73` 的 `_populate_timeline_store()` 只替换 `_segments` 和 `_segment_order`，没有维护时间、路径和边界索引。`VideoTimelineStore.locate_passage()` 已依赖这些索引。

本次使用原辅助函数做了最小复现：

| 同一组 3 个录像段 | 候选数 | 查询状态 |
|---|---:|---|
| 原基准辅助函数填充后 | 0 | `no_segments` |
| 仅补建索引后 | 1 | `missing_file` |

复现没有创建媒体，故第二行的 `missing_file` 符合预期；第一行错误地走了无录像快速返回。现有时间线基准以及依赖该辅助函数的名单场景，不能直接作为真实定位性能依据。

此外，名单基准使用 32×32、3 帧 MJPG 文件，声明的时间范围远长于实际媒体，只适合有限的控件开销检查。它没有覆盖真实主窗口中的接收、派生发布、筛选状态更新和边录边判。

### B. 界面线程仍执行扫描、发布和同步写盘

`review_window.py:5401` 的 `_flush_passage_batch()` 由 Qt 定时器调用，依次执行 `ring_buffer.scan()`、归档发布、窗口注册和共享片段发布，最后才刷新名单。

周期扫描虽然已经后台化，但 `_on_capture_refresh_finished()` → `_apply_capture_refresh_state()` → `_publish_ready_windows()` 仍在界面侧执行窗口保护和片段发布，见 `review_window.py:5704`、`:5755`、`:5092`。

这些调用会进入以下同步 I/O：

- `review_recorder.py:1513`：pin 日志与文件锁；注册和刷新窗口均可能到达这里。
- `review_recorder.py:2117`：播放清单写入、`fsync` 和替换。
- `video_timeline.py:413`：时间线追加与 `fsync`。
- `review_clip.py:263`：clip/binding 日志追加与 `fsync`。

启动构造过程也会同步补发历史归档，见 `review_window.py:2768`。已确认的是慢磁盘、锁竞争和媒体探测可以阻塞界面的路径，实际延迟仍需基准测量。

### C. 增量更新容易退化为整场工作

`passage_review.py:5861` 的 `refresh_events()` 在以下情况回退 `refresh()`：变化超过 64 条、撤回事件、组别变化，或当前存在组别/搜索/状态筛选。确认记录时启用状态筛选也会全刷新，见 `:8127`。

全刷新会重新计算批次、定位和状态，再为所有可见行创建 9 列 `QTableWidgetItem`，见 `:5771`、`:5096`、`:5022`。即便走增量路径，也包含逐事件线性找行和全表重新编号。

另外 `_cached_lookup()` 把整个 binding store 的 revision 纳入每条事件的缓存键，见 `:4761`。一次无关绑定更新后，其他事件的定位缓存在下次访问时也会失效；与全刷新叠加时会放大成本。

### D. 整场胶卷的来源刷新仍包含全量文件检查

`passage_review.py:6841` 每次更新胶卷都调用 `recording_sources()`；后者遍历全部时间线段并执行 `is_file/stat`，见 `race_filmstrip.py:133`。`set_sources()` 的签名短路发生在这些检查之后；新增来源时还会重建 `RaceRecordingIndex`。

因此“只解码可见缩略图”已实现，但来源发现和状态计算尚未同样做到增量化。长赛事、历史目录和慢存储下的实际占比需要单独测量。

### E. 新旧胶卷与播放任务的资源调度边界不一致

`video_filmstrip.py:1023` 的操作忙碌状态只停止旧窗口预取。当前整场胶卷 `_load_visible()` 直接启动 `RaceThumbnailWorker`，见 `race_filmstrip.py:1064`，没有接入同一忙碌策略。

当前播放缓存和胶卷缓存已有各自上限，但普通播放、反向预取、暂停原图、整场缩略图等仍独立调度。应测量同时开启的 capture 数、线程数和解码等待，确认资源竞争程度。

### F. 界面和运行状态仍高度耦合

`review_window.py` 共 6,877 行，`FinishReviewWindow` 继承 `PassageReviewSurface`；后者所在的 `passage_review.py` 共 9,500 行。两类分别有 109 和 219 个方法。已有控制器，但主窗口仍持有录制、发布、归档、重连、名单和画面状态。

行数反映维护负担，不直接证明性能问题。拆分应服务于上面已找到的执行与状态边界，避免仅移动文件而没有改善响应。

## 4. 目标结构

```text
接收器 → 过线日志 durable commit → 事件变化通知
                                  ├→ UI 数据投影 → 名单增量显示
                                  └→ 赛事后台任务队列
                                      扫描 / pin / 发布 / binding / cleanup
                                           ↓
                                  已提交的变化结果与只读快照
                                           ↓
                                     UI 应用局部变化

操作员 → 当前复核状态 → 播放请求调度
                          ├→ 当前机位精确帧 / 连续播放
                          ├→ 当前可见缩略图
                          └→ 空闲邻近预取
```

约束：

1. 过线日志继续先持久化、后通知。证据准备失败不能阻止已接收事件及其更正、撤回出现在名单中。
2. 扫描、pin、派生发布和 cleanup 在同一个赛事上下文内有明确串行所有权；不要直接把目前所有方法同时放入线程池。
3. GUI 使用只读快照与变化结果；不能通过读取后台持有的长时间写锁间接阻塞界面。
4. 任务携带赛事上下文、generation、事件 revision 和机位。旧结果不能更新新赛事、覆盖新 revision 或恢复已撤回事件。
5. 重复扫描可合并；证据写入、撤回和保护关系不能按普通预览的“只保留最后请求”策略丢弃。队列设置上限，积压时停止低优先级工作，依靠已持久化事件进行恢复与补处理。
6. 保留现有顺序：保护媒体 → 清单原子落盘 → 时间线 → clip/binding → 释放临时保护。中途失败重试必须幂等；cleanup 不能越过待处理的证据保护任务。
7. 后台可以更新录像可用状态，操作员的身份、当前原帧、浏览位置和未确认标线由复核状态控制。例外是撤回/失效必须按现有规则明确处理，不能继续确认无效上下文。

## 5. 分阶段实施

| 阶段 | 改动与交付 | 完成条件 |
|---|---|---|
| 0：可信测量 | 修正基准数据构造，优先使用正式写入/加载路径；增加命中正确性断言；扩展 RuntimeMetrics，记录 GUI 事件循环延迟、接收到名单延迟、文件检查、发布写盘、缓存命中、worker 数量 | 基准实际经过预期定位路径；保存硬件、媒体、SHA、冷/热缓存和原始样本 |
| 1：解除 UI 的 I/O 阻塞 | 扩展或提取现有 CaptureRefreshWorker 的赛事任务队列；将注册/pin、共享发布、归档补发及 cleanup 整合到明确所有者；UI 先应用已持久化事件，再显示证据准备结果 | 慢扫描/写盘注入时 UI 仍可输入；撤回、revision、失败重试、赛事切换、停止录制和清理顺序正确 |
| 2：增量投影与索引 | 建立 event_id → 行/状态投影，筛选中也应用插入/修改/撤回；binding 缓存按事件/机位失效；录像来源按新增/封口/删除变化更新；文件检查在后台执行 | 无关单条更新不触发全场定位和整表重建；来源不变时不重复检查全部文件；选人、滚动、当前帧保持既有语义 |
| 3：解码调度和操作反馈 | 整场胶卷接入忙碌/空闲状态；精确判读优先，可见缩略图与邻近预取分开；统计并限制总缓存和并发 capture；保留现有请求取消、原帧校验 | 播放/逐帧时预取让出资源，停止后恢复；旧请求不回写；连续操作后资源稳定 |
| 4：收拢架构与现场验收 | 沿已形成的后台队列、名单投影和复核状态边界抽取模块；将设置/预检对话框独立；更新历史方案实施状态；完成真实设备及 EXE 验收 | 主窗口以界面组装和信号连接为主，旧数据可读，录制和证据正确性无回归 |

阶段 2 先实现事件变化投影与有针对性的控件更新；如果固定 5,000 人场景仍因控件创建/布局超标，再把 `QTableWidget` 换为 `QTableView + QAbstractTableModel`，配合筛选模型。不要把更换控件作为后台 I/O 改造的前置条件。

每阶段单独提交和验证。首批建议只做阶段 0、1，先建立可复现的响应改善证据，再扩大重构范围。

## 6. 易用性设计

- 接收到新名单/更正立即可见；对应录像尚未准备完成时显示“录像准备中”，失败时提供明确原因与重试入口。
- 保留当前单击换身份、双击定位芯片时间、胶卷点击定位原帧、F 放大、Esc 返回的操作语义。
- 搜索/组别/已判筛选持续有效，新事件到达时不重置筛选和滚动位置。
- 继续区分“录像处理中”“已可回看”“真实缺口”“解码失败”；不得把未完成处理显示为证据缺失，也不得把缩略图加载完成当作完整性保证。
- 利用现有状态区域反馈，不增加持续打断判读的弹窗。性能诊断通过日志或按需导出提供。

## 7. 验收方法与目标

以下是建议目标，不是本次已测得的结果。计时基于固定现场设备，保留 p50/p95/max 和失败次数。

| 指标 | 建议目标 |
|---|---|
| 持续接收和归档时 GUI 事件循环延迟 | p95 < 100 ms；避免持续超过 200 ms 的界面停顿 |
| 已持久化事件到名单可见 | p95 ≤ 250 ms，包含当前批次合并等待；与录像就绪延迟分别统计 |
| 5,000 人名单中单条变化/筛选操作 | 增量应用 p95 < 100 ms；搜索响应 ≤ 150 ms |
| 同一共享片段切换到目标首帧 | p95 ≤ 150 ms |
| 跨片段首帧 / 缓存未命中精确帧 | SSD 上分别 p95 ≤ 500 / 400 ms |
| 缓存命中逐帧 | p95 ≤ 50 ms |
| 应用私有内存 | 典型配置以 ≤ 1 GB 为目标；缓存、线程、capture 数保持有界 |

性能样本覆盖 500/2,000/5,000 人、单/双机位、5 秒 50 人冲刺、revision 重发/更正/撤回、筛选中接收、历史归档首次打开、实时 TS 切换归档、连续滚动及反向逐帧。采用真实有帧号与时间戳的媒体，避免只看 fake worker 的耗时。

正确性回归复用现有测试，重点增加改造边界处的行为测试：

- 更正/撤回到达时，旧后台结果不能覆盖当前状态；重复事件不能生成重复绑定。
- 发布任一步失败后保持媒体保护，恢复后可幂等完成；清理不得删除已确认/待处理证据。
- 文件缺失、边界、校时和原帧标记结果与改造前一致。
- 打开/关闭判读窗口、切换赛事、录制停止期间，没有旧线程或结果进入新上下文。
- 先完成 15–30 分钟录制与判读并行验证，发布候选再做数小时运行及真实断网/磁盘异常验证。
- 运行现有 Ruff、相关测试和全量 pytest；涉及生命周期或打包时补 EXE smoke。真实 RTSP/USB/共享目录验收另行记录。

## 8. 条件性后续工作

SQLite/快照仅在赛事日志加载时间和规模实测超标后评估。OpenCV 后端替换、FFmpeg 关键帧后端或硬解仅在调度与刷新优化后，真实媒体解码仍不达标时做独立 A/B 原型。两者都不属于首轮必做项目。


本次验证范围说明已移至第 9 节实施与验证记录。

## 9. 实施与验证记录

首轮提交 `2897893` 完成后台证据流水线、录制目录缓存、UI 延迟指标、增量名单投影、胶卷忙碌调度和日志读写锁分离；当时尚未完成全部 Phase 0–3。本轮继续补齐如下代码边界：

- 阶段 0：增加共享 capture 数量/峰值、排队时间、图像缓存字节/命中，以及目录文件检查统计；视频基准保存帧序列、帧间隔、超时、SHA、源码摘要及缓存说明。
- 阶段 1：定点回放和视觉异常回放准备、pin/清单清理、停止录像及最终发布进入同一后台串行队列。显式操作等待时继续 Qt 事件分发，延后会改变上下文的回调；日常刷新仍为异步。归档受影响事件的文件检查、状态栏磁盘空间检查退出 UI 线程。
- 阶段 2：时间线提供有界 revision 变化历史，目录只处理新增/封口段；变化历史过期时恢复全量快照。旧文件按每次最多 32 个的轮询重新检查，显式删除立即失效，外部删除/恢复最终可见。
- 阶段 3：1 倍及以下反向播放按原帧连续显示，边界复用正在解码的预取，暂停/定位仍取消旧请求。播放、反向预取及两套胶卷共享 6 个 capture（后台最多 2 个）和 256 MiB 保留图像预算，优先淘汰预取/缩略图。单个播放缓存仍为 64 MiB。
- 阶段 4：设备设置、赛事选择及预检界面抽出到 `launch_dialog.py`，保留公开导入入口；删除主窗口中已被证据流水线替代的注册/分组/发布代码。主窗口仍包含运行状态和会话协调逻辑，尚不能称为仅负责界面组装。

代码提交：`4f2d4a5`（解码调度）、`29fd197`（目录增量）、`734af40`（后台任务及界面拆分）。验证结果：本轮全量 pytest 为 770 passed、1 skipped；补充资源压力测试和缓存竞争修复后相关子集为 102 passed。Ruff、compileall、PyInstaller 发行目录边界检查和 `--smoke-test` 通过。500/2,000/5,000 条时间线基准使用生产加载路径并断言定位正确。首轮同机合成 5,000 条名单单条增量更新 p95 为 46.167 ms，带搜索筛选为 69.446 ms；旧实现分别为 187.227 ms、1722.54 ms。该结果用于回归比较，不代表现场设备承诺。

合成压缩媒体（8 秒、25 fps、GOP50）复测中，H.264 1080p 和 H.265 1440p 均连续显示 20 帧，没有跳帧、重复或乱序；`video-benchmark-final.json` 分别约 21.2 fps、13.9 fps，H.265 首轮为 2.8 fps 且跳帧。当前仍未达到源视频 25 fps，单次样本不代表长期流畅度。原始结果保存在本地 `artifacts/issue3/video-benchmark-*.json`。

未完成项：真实 RTSP/USB、CycleRace、共享目录、15–30 分钟录制判读并行、数小时运行及断网/磁盘异常验收；真实媒体下的 GUI/内存目标验证；阶段 4 更进一步的运行状态/会话控制器收拢。共享预算目前覆盖判读播放与胶卷，独立视觉检测、活动分析和媒体时长探测仍需统筹；256 MiB 不包含当前展示帧、全分辨率临时帧、OpenCV 内部缓冲和 Qt 待投递信号。因此本计划仍未全部完成，不能关闭现场验收项。SQLite、替换解码后端和硬解依第 8 节按测量触发。
