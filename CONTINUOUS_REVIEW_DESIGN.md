# FinishReview 连续录像复核方案

状态：讨论确认稿，作为后续实现和验收依据。本文只定义产品逻辑与技术边界，不包含代码修改。

## 1. 核心定位

FinishReview 的普通相机复核应当采用“硬盘录像机回放”模型：

- 录像端持续录制正在进行的比赛。
- 复核端只读取已经写入的录像，并维护独立的回放游标。
- 后台可以继续写盘、建立时间索引和预取后续录像，但不能改变操作员当前选手、当前录像、当前时间或当前画面。
- 前台的任何跳转必须来自操作员的明确动作。

系统不是“自动识别运动员后控制前台”的分析器。芯片记录、连续录像和人工确认是三个相互关联但彼此独立的数据层。

## 2. 已确认的界面语义

### 2.1 左侧芯片名单

- 单击运动员：只切换当前身份，不改变上下方录像时间。
- 上下方向键：与单击相同，只切换身份，不改变录像。
- 双击运动员：明确执行“定位芯片时间”。
  - 读取该运动员的芯片绝对时间。
  - 在相机 1 的整场连续录像中定位。
  - 上方胶卷和下方机位 1 同时进入同一个绝对时间。
  - 不打开旧的短证据录像，不弹出定点回放窗口。
- 已确认短证据通过单独的“查看确认录像”入口打开，不能复用名单双击。

### 2.2 上方时间胶卷

- 胶卷代表相机 1 整场连续普通录像，界面按固定两分钟窗口分页。
- 胶卷单击：下方机位 1 跳到所点位置的同一绝对时间，不自动切换左侧运动员。
- 胶卷双击：在相同时间进入精确判读或放大机位 1，仍不自动归属运动员。
- 前一窗口、后一窗口只能由操作员主动切换。
- 后台预取完成不能自动切换当前窗口。

### 2.3 下方机位 1

- 机位 1 是逐帧精确判读画面。
- 机位 1、时间胶卷必须使用同一个连续录像来源和同一个绝对时间域。
- 播放、暂停、拖动和逐帧操作只改变回放游标，不改变左侧身份。
- 新芯片记录到达、时间索引更新和后台预取都不能抢占当前画面。

## 3. 统一时间模型

所有定位必须遵守同一个公式：

```text
录像绝对起点 + 媒体内相对位置 = 真实绝对时间
```

校时偏移只能应用一次。禁止把短证据片段的位置、连续录像的位置和芯片时间直接互相当作同一种数值。

每次显示或保存位置时至少要明确：

- 相机编号；
- 连续录像文件或段编号；
- 录像绝对起点；
- 媒体内位置；
- 换算后的绝对时间；
- 使用的校时版本。

149 和 67 即使只相差 262 毫秒，也必须分别保存独立标记，不允许根据“距离最近”自动互相归属。

## 4. 两分钟窗口处理模型

两分钟只是复核界面的分页单位，不是录像边界，也不能造成录像时间断开。

### 4.1 当前窗口准备

进入一个两分钟窗口后，系统按以下顺序处理：

1. 固定窗口的绝对起止时间。
2. 解析覆盖该窗口的连续录像文件和切片边界。
3. 检查时间覆盖是否完整。
4. 顺序解码该窗口，验证媒体可读性和帧时间连续性。
5. 在同一次顺序解码中生成基础缩略图，避免为了缩略图再次随机读取录像。
6. 当前窗口全部完成后，才显示“当前两分钟已就绪，可以处理”。

“第一张缩略图出来”不能再被称为“已就绪”。准备期间应显示明确进度，例如：

```text
正在准备 14:36:00–14:38:00：37 / 61 个预览点
```

### 4.2 当前窗口判读

窗口进入“已就绪”后：

- 缩略图集合、时间原点和录像绑定保持不变。
- 操作员可以浏览胶卷、播放机位 1、逐帧标记和确认。
- 后台不能把预取结果直接插入当前胶卷，避免画面持续刷新。

### 4.3 后台预取

操作员处理当前窗口时，后台低优先级准备下一个两分钟窗口：

```text
当前显示：14:36:00–14:38:00
后台预取：14:38:00–14:40:00
```

预取结果进入独立缓存。操作员主动进入下一窗口时：

- 已预取完成：立即显示。
- 尚未完成：显示准备进度，完成后再进入可处理状态。
- 预取失败：显示具体错误，不能静默显示空白窗口。

后台最多预取一个后续窗口，并限制缓存窗口数量，避免长时间比赛占满内存。

## 5. 两类“断点”必须分开

### 5.1 真实录像断点

录像文件、切片或帧时间本身缺失，例如：

```text
前段结束 14:37:20.000
后段开始 14:37:22.500
```

这种情况下缺少 2.5 秒真实证据。系统必须：

- 标出准确的断点起止时间；
- 显示“录像不完整，不能保证该时段无运动员”；
- 不允许窗口进入“完整可处理”状态；
- 如有其他机位，提示检查其他机位；
- 不能把“没有录像”解释成“没有运动员通过”。

如果运动员正好在真实断点通过，软件不能恢复不存在的画面，只能可靠地暴露证据缺失。

### 5.2 缩略图采样间隔

录像可能完整，但两张缩略图之间没有展示。例如每两秒一张时，运动员可能只在中间短暂出现。

这不是录像断点。基础缩略图只用于快速定位，不能作为“这段时间绝对无人通过”的证明。

要实现不漏判，最终依据必须是机位 1 的连续帧。建议增加“已人工查看范围”轨道：只有实际连续播放或逐帧覆盖的时间段才标记为已查看，跳过的区间保持未查看状态。

## 6. 取消活动分析

现有活动分析只计算相邻画面的像素变化：

- 不能识别运动员；
- 不能判断芯片是否漏读；
- 不能验证录像是否完整；
- 摄像机抖动、阴影和曝光变化都会干扰；
- 会额外解码同一段视频并与人工回放争用资源。

因此从运行流程中取消：

- 不启动活动分析工作线程；
- 不显示活动强度曲线；
- 不使用活动结果生成或过滤缩略图；
- 不使用活动结果切换运动员或录像位置。

必须保留的是“录像完整性检查”。它只验证时间覆盖、帧时间连续性和解码错误，不分析画面内容，不判断画面里是否有人。

## 7. 解码和资源优先级

优先级从高到低必须固定：

1. 现场录像写盘；
2. 操作员正在使用的机位 1 播放、逐帧和定位；
3. 当前两分钟窗口首次准备；
4. 下一窗口后台预取；
5. 其他非必要维护任务。

当操作员播放、拖动或逐帧时，后台预取应暂停或主动让出资源；操作停止一小段时间后再恢复。后台任务无论成功、失败还是完成，都只能更新自己的状态和缓存，不能改变前台回放游标。

正在写入且尚未封口的最新录像窗口不能声明“完整就绪”。复核端可以停留在落后实时录制头的已完成窗口，录像端继续录制未来内容。

## 8. 相机 1 的连续标记轨道

相机 1 上的标记属于整场连续时间轴，不属于当前界面的临时播放器状态。

每条标记至少包含：

- 唯一标记编号；
- 相机编号；
- 绝对时间；
- 对应媒体段和媒体内位置；
- 标记坐标；
- 运动员编号，或“未知/待补录”；
- 已确认、待确认等状态；
- 校时版本和修改时间。

显示规则：

- 当前运动员标记高亮。
- 同一窗口内其他已确认运动员的标记继续保留。
- 切换左侧运动员不能删除前一个人的标记。
- 拖动录像、切换文件后，按照绝对时间重新加载当前窗口的全部标记。
- 149、67等时间非常接近的标记要错位显示编号，不能互相覆盖。
- 胶卷发现无芯片运动员时，先保存为“未知/待补录”，以后人工关联，禁止自动归给最近的芯片运动员。

这条标记轨道在使用效果上模拟高速线性相机的连续照片：所有运动员都留在同一条时间轴中，只是普通相机的底层证据仍然是连续视频帧。

## 9. 前后台状态隔离

至少需要维护三个独立状态：

```text
录制状态：当前写到哪里
复核状态：操作员正在看哪里
预取状态：后台正在准备哪里
```

禁止用一个“当前时间”变量同时代表三者。

复核窗口应使用不可变上下文标识，至少包括录像路径、窗口起止时间和生成编号。后台旧任务即使延迟返回，也必须因为上下文编号不匹配而被丢弃，不能覆盖新窗口。

## 10. 实施顺序

### 第一阶段：先稳定交互和时间

1. 名单单击/方向键只换身份，不动录像。
2. 名单双击定位整场连续录像的芯片绝对时间。
3. 移除双击后的短录像弹窗调用。
4. 胶卷单击只定位机位 1，不自动选人。
5. 统一上下画面的绝对时间换算，并保证校时偏移只使用一次。

### 第二阶段：取消活动分析

1. 停止活动分析工作线程和调度。
2. 移除活动曲线界面。
3. 保留胶卷更新调度，但不能再触发像素变化扫描。

### 第三阶段：两分钟准备与预取

1. 用一次顺序解码同时完成完整性验证和缩略图生成。
2. 建立“准备中、已就绪、有断点、失败”窗口状态。
3. 当前窗口完整后才提示可以处理。
4. 建立独立的下一窗口预取缓存。
5. 操作员忙时暂停预取。

### 第四阶段：连续标记和漏芯片流程

1. 将相机 1 标记改为绝对时间轴上的多标记集合。
2. 当前标记高亮，历史标记持续显示。
3. 支持未知运动员标记和后续人工关联。
4. 把短证据查看与连续时间轴判读彻底分离。

## 11. 必须通过的验收场景

1. 单击149再按下键到67，机位1和胶卷时间均不变化。
2. 双击149，上下画面都进入149芯片时间对应的连续录像，不弹短录像。
3. 双击67，上下画面都进入67芯片时间；149和67分别保留标记。
4. 胶卷点击两人之间的无芯片位置，只移动机位1，不自动选择149或67。
5. 当前两分钟准备未完成时不能显示“可以处理”。
6. 当前两分钟存在真实录像断点时，必须显示断点且不能标为完整。
7. 当前两分钟判读期间，新运动员到达和后台索引更新不能改变当前画面。
8. 后台预取下一窗口时，当前窗口不刷新、不跳时、不换录像。
9. 正在播放或逐帧操作时，后台预取主动让出资源。
10. 重启程序后，已确认运动员和未知运动员的时间轴标记仍然存在。
11. 旧后台任务完成后不能覆盖操作员已经切换到的新窗口。
12. 活动分析线程不再启动，正式录像进程不受影响。

## 12. 安全边界

- 软件只能报告和标记真实录像缺失，不能恢复未录到的画面。
- 静态缩略图无论多密，都不能替代连续帧复核。
- 自动候选只能作为提示，不能自动生成正式成绩或自动归属运动员。
- 现场录像进程始终优先，复核优化不得停止或重启正式录制。

